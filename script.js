let index = 0;

function changeColors () {
     const colors = ["red", "pink", "beige", "yellow", "purple", "brown"];

     document.getElementsByTagName("body")[0].style,background = colors[index++];

     if(index > colors.lenght -1)
}